[advisories]
# RUSTSEC-2023-0071: Marvin Attack timing sidechannel in rsa 0.9.x
# Pulled in transitively via sqlx-mysql (through sqlx macros feature).
# This app uses PostgreSQL only; the MySQL code path is never exercised.
# No upstream fix available as of 2026-05. Revisit when sqlx drops rsa or a
# patched rsa release is published.
ignore = ["RUSTSEC-2023-0071"]
