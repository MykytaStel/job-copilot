# Testing Rules

Must-have tests for new slices:
- domain conversion tests
- matching/rules tests
- ranking merge tests
- API handler tests where practical
- frontend query invalidation behavior for navigation-sensitive pages

For source filtering:
- tests must verify source inclusion/exclusion works as structured filters
